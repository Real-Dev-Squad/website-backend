const { DINERO, NEELAM } = require("../../../constants/wallets");

/* Import fixtures
 *
 * Sample wallet for tests
 *
 * @return {Object}
 */

const currencies = {
  [DINERO]: 1000,
  [NEELAM]: 2,
};

module.exports = currencies;
